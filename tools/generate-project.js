/**
 * Phase 0: S3M tick extraction tool.
 *
 * Parses MUSIC0.S3M and MUSIC1.S3M from the original SecondReality repo,
 * extracts tick-accurate beat timestamps and part boundaries from MAIN/SCRIPT,
 * and writes assets/project.json with authoritative timings.
 *
 * Usage:
 *   SR_REPO=../SecondReality node tools/generate-project.js
 *
 * Prerequisites:
 *   - Clone https://github.com/mtuomi/SecondReality
 *   - npm install libxmp-wasm
 *
 * TODO: Implement once libxmp-wasm API is verified.
 * See docs/PROJECT_BRIEF.md for the full extraction algorithm.
 */

console.log('Phase 0 extraction tool — not yet implemented.');
console.log('Using placeholder assets/project.json for development.');
process.exit(0);
